# 晨间设备自检

一个基于 React、TypeScript、Vite、Tailwind CSS 的离线优先 PWA。全部数据仅保存在 `localStorage`，无后端依赖。

## 功能

- 每日单次状态记录，可随时修改当天结果
- 连续天数、月度统计、热力图、状态占比图
- 成就徽章系统
- 导出 / 导入 JSON
- 深色模式
- 可安装，支持离线访问

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
npm run preview
```
